apiVersion: 1

datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    # Reachable over the logs-net overlay network, never exposed publicly.
    url: http://loki:3100
    isDefault: false
    editable: false
    jsonData:
      maxLines: 2000
      timeout: 60
